<h1>Editing photo</h1>

<%= render :partial => 'form' %>

<%= link_to 'Show', @photo %> |
<% back_path = if params[:album_id]
                 album_path(params[:album_id])
               else
                 photo_path(@photo)
               end
%>
<%= link_to 'Back', back_path %>
